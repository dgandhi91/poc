node {
    checkout scm
    stage('Build') {
        echo 'Build'
        sh (${WORKSPACE}/'build')
    }
    stage('Test') {
        echo 'Test'
    }
    stage('Deploy') {
        echo 'Deploy'
    }
}